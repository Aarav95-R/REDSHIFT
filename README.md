# Red Shift - multi-asset forward tracker

Published dashboard for the tf_55_25_20 forward tracker. This repository
contains **only the generated page**; the strategy, the study reports and
the generator live in a private repository that is not published.

Regenerate and stage with `py -3 publish_pages.py` from the strategy repo,
then `git push` from here.
